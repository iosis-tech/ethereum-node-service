apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ethereum-pvc
spec:
  storageClassName: rook-ceph-ec-hdd-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2000Gi